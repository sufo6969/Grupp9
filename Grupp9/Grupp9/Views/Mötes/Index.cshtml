@model IEnumerable<Grupp9.Models.Möten>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.MötesBeskrivning)
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
      
        <td>
            @Html.DisplayFor(modelItem => item.MötesBeskrivning)
           @Html.Action("VisaDatum", "Mötes", new { Mötesid= item.MöteId })
        </td>
        <td>
            @Html.ActionLink("Bjud in", "BjudIn", new { id=item.MöteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MöteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MöteId })
        </td>
    </tr>
}
</table>
